import Table from "./Editable";
import DraggableTable from "./draggable/Draggable";
import { flatten, unflatten } from "react-base-datagrid";

export { Table, DraggableTable, flatten, unflatten };
export default Table;
